{
  "name": "golden-hopper-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ kontrol-apa/golden-hopper-subgraph",
    "create-local": "graph create --node http://localhost:8020/ kontrol-apa/golden-hopper-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ kontrol-apa/golden-hopper-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 kontrol-apa/golden-hopper-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.31.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
